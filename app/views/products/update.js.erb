$("#myupdateproduct_<%= @product.id %>").modal('hide');

$("#product_<%= @product.id %>").fadeOut(500, function(){

	$(this).remove();
	$("#container_products").prepend('<%= j render @product %>');

});
